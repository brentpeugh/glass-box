# Real key lives ONLY in Netlify env config (Site settings → Environment variables),
# never in the repo. This file documents the name; it holds no secret.
# Local live testing: `netlify dev` reads the key from your linked Netlify env,
# or create .env.local (gitignored) with:
ANTHROPIC_API_KEY=sk-ant-...
